# Tasks — F-TEST-007

## Milestone 1

- [x] **TASK-001** — Set up `FileUploaderComponent` tests with FormBuilder documents array, parent `totalAttachmentSize`, mocked `MatSnackBar`. Covers `@R-05.1`.
- [x] **TASK-002** — Test that an unaccepted extension (e.g. `malware.exe`) does not add a document and opens the unsupported-type snackbar. Covers `@R-05.2`.
- [x] **TASK-003** — Keep `should create` or equivalent; do not convert the spec back into a stub-only file. Covers `@R-05.1`.
- [x] **TASK-004** — Run the uploader spec (headless if available); append `docs/pr-evidence.md`. Covers `@R-05.1`.

## Backlog

- [ ] VULN-002 server MIME — **not this slice**
- [ ] F-TEST-001 / F-TEST-005 / F-TEST-006 — **not this slice**
