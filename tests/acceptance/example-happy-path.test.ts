// criterion: @R-00.1
// Acceptance placeholder — replace with real tests derived from spec/features/example-happy-path.feature.
// Deliberate-blindness lite: name the criterion; do not read implementation to invent expectations.

import { describe, it, expect } from "vitest";

describe("R-00.1 example happy path", () => {
  it.todo("maps to spec/features/example-happy-path.feature — successful completion");
});
